#Get information from card to display on interface and lcd 
Raspberry transmit the image to the Server and display the information on the interface

INSTALL
Raspberry
  pip
    PIP for python2 - sudo apt-get install python-pip
    PIP for python3 - sudo apt-get install python3-pip
  opencv
    pip install opencv-python
  imutils
    pip install imutils
  rpi_lcd
    https://www.youtube.com/watch?v=DHbLBTRpTWM
 
 Ubuntu
   pip
    PIP for python2 - sudo apt-get install python-pip
    PIP for python3 - sudo apt-get install python3-pip
  opencv
    pip3 install opencv-python
  imutils
    pip install imutils
  scikit-image
    pip3 install scikit-image
  teseract
    sudo apt-get install tesseract-ocr
  pyqt5
    pip install PyQt5
 
 Before Run
   change host in client.py to be the same as gui_AI_1.py
   run gui_AI_1.py before running client.py 
 
  
